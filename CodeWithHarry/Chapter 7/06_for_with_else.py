l = [1, 7, 8]

# Iterates through each element of the list
for item in l:
    print(item)
    
# The else block executes after the loop
# finishes normally (without encountering a break).
else:
    print("done")
    # Output:
    # done